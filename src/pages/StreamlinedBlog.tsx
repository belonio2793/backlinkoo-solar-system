/**
 * Streamlined Blog Page
 * Single page that handles all blog generation workflows efficiently
 */

import { StreamlinedBlogGenerator } from '@/components/StreamlinedBlogGenerator';

export default function StreamlinedBlog() {
  return <StreamlinedBlogGenerator />;
}
