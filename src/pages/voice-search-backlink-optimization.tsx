import React from 'react';
import { GenericPageTemplate } from '@/components/GenericPageTemplate';

const VoiceSearchBacklinkOptimization: React.FC = () => {
  const title = "";
  const subtitle = "Complete guide on ";
  const htmlContent = `Placeholder content generated. Expand manually.`;
  const keywords = "voice, search, backlink, optimization, backlink strategy, link building, SEO";
  
  return (
    <GenericPageTemplate
      title={title}
      subtitle={subtitle}
      htmlContent={htmlContent}
      keywords={keywords}
      description={subtitle}
    />
  );
};

export default VoiceSearchBacklinkOptimization;
