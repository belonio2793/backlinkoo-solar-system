/**
 * Force Text Spacing - DISABLED
 * No autoformatting applied
 */

export function forceTextSpacing(text: string): string {
  // Return text unchanged - no autoformatting
  return text;
}

export function createSpacedText(text: string): string {
  // Return text unchanged - no autoformatting
  return text;
}
